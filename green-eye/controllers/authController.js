const { model } = require("mongoose");

const login = async (req,res) => {
    try{

    }catch(err){
        console.log(err);
        
    }
}
const logout = async (req,res) => {
    try{

    }catch(err){
        console.log(err);
        
    }
}

module.exports = {
    login,
    logout
}